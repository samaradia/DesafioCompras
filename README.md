## 📌 Descrição do Projeto

Este projeto simula o funcionamento básico de um **cartão de crédito**, desenvolvido em **Java**, com foco no aprendizado de lógica de programação e orientação a objetos.

O programa permite que o usuário:
- Informe o limite do cartão
- Realize compras enquanto houver saldo disponível
- Visualize todas as compras realizadas
- Consulte o saldo final do cartão

Durante a execução, o sistema verifica se há saldo suficiente antes de autorizar cada compra, utilizando estruturas de repetição (`while`), listas (`List`), e classes para organizar as responsabilidades do código.

O projeto foi desenvolvido com o objetivo de **praticar conceitos fundamentais do Java**, como:
- Classes e objetos
- Encapsulamento
- Listas (`ArrayList`)
- Estruturas de decisão (`if/else`)
- Estruturas de repetição (`while` e `for-each`)
- Entrada de dados com `Scanner`

Projeto voltado para **fins educacionais e aprendizado**.
